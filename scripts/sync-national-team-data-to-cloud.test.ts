import { describe, expect, it } from 'vitest';
import { createMemoryCloudPersistenceAdapter } from '../apps/api/src/persistence/memory-cloud-persistence-adapter.js';
import { runNationalTeamCloudSync } from './sync-national-team-data-to-cloud.js';
describe('national-team cloud sync command',()=>{it('prints a stable summary',async()=>{const messages:string[]=[];const result=await runNationalTeamCloudSync({localRepository:{loadSnapshot:async()=>({snapshotId:'s1',generatedAt:'2026-07-02T00:00:00.000Z',importedAt:'2026-07-02T00:00:00.000Z',sources:[],matches:[]})},adapter:createMemoryCloudPersistenceAdapter(),ownerProfileId:'owner-primary',log:(message)=>messages.push(message)});expect(result.matchCount).toBe(0);expect(messages).toEqual(['Synced national-team snapshot s1: 0 matches to supabase-postgres']);});});
