import type { CloudBackupEnvelope, CloudBackupEnvelopeV2 } from '@miraichi/shared';
import { buildApiUrl } from '../config/client-env.js';
import type { FetchLike } from './cloud-persistence-service.js';

function validate(value:unknown):value is CloudBackupEnvelope{if(typeof value!=='object'||value===null)return false;const item=value as Partial<CloudBackupEnvelope>;const common=['miraichi.cloud-backup.v1','miraichi.cloud-backup.v2'].includes(String(item.schemaVersion))&&typeof item.exportedAt==='string'&&typeof item.ownerProfileId==='string'&&Array.isArray(item.drafts)&&Array.isArray(item.bets)&&Array.isArray(item.bankrollAccounts)&&Array.isArray(item.bankrollLedgerEntries);if(!common)return false;if(item.schemaVersion==='miraichi.cloud-backup.v1')return true;const v2=item as Partial<CloudBackupEnvelopeV2>;return Array.isArray(v2.disciplineConfigs)&&Array.isArray(v2.settlementEvents);}
export async function exportCloudBackup(fetcher:FetchLike=fetch):Promise<CloudBackupEnvelopeV2>{const response=await fetcher(buildApiUrl('/api/v1/backups/export'),{method:'POST'});if(!response.ok)throw new Error('Backup export failed');const payload=await response.json();if(!validate(payload)||payload.schemaVersion!=='miraichi.cloud-backup.v2')throw new Error('Invalid backup response');return payload;}
export async function importCloudBackup(payload:unknown,fetcher:FetchLike=fetch):Promise<void>{if(!validate(payload))throw new Error('Unsupported backup schema');const response=await fetcher(buildApiUrl('/api/v1/backups/import'),{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});if(!response.ok)throw new Error(response.status===409?'Backup import conflict':'Backup import failed');}

